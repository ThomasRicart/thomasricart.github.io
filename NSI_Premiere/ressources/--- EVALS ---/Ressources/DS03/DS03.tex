\documentclass[a4paper,12pt,openany]{book}
\input{../../../preambule/preambule}

% en-tête
\rhead{\footnotesize{\textsf{1ère NSI}} }%droit
\lhead{Nom - Prénom:}
\begin{document}

\begin{center}\begin{large}
DS03A - Structures IF / FOR / WHILE
\end{large}\end{center}



\begin{enumerate}
	\item 
	
	\begin{lstlisting}[numbers=none]
def fonc1(a,b):
    if a < b:
        return b - a
    else:
        return a * b
\end{lstlisting}
	
	Que renvoie la commande \verb+fonc1(4,3)+ ? \dotfill
	
	\medskip
	
		Que renvoie la commande \verb+fonc1(3,4)+ ? \dotfill

	\item 
	\begin{lstlisting}[numbers=none]
def fonc2(n):
    a = 1
    for i in range(n):
        a = a * 2
    return a
\end{lstlisting}
	
	Que renvoie la commande \verb+fonc2(4)+ ? \dotfill

	\medskip
	
	\item 
	\begin{lstlisting}[numbers=none]
def fonc3(une_liste):
    a = 0
    for elt in une_liste:
        a = a + elt
    return a
\end{lstlisting}
	
	Quelle est la valeur renvoyée par \verb+fonc3([1,3,4,6])+? \dotfill

	\medskip
	
	\item 
	\begin{lstlisting}[numbers=none]
def fonc4(une_liste):
    n = len(une_liste)
    sortie = []
    for i in range(n):
        if une_liste[i] % 2 == 0:
            sortie.append(i)
    return sortie
\end{lstlisting}
	
	Quelle est la valeur renvoyée par \verb+fonc4([1,3,4,6])+? \dotfill
	
	\medskip
	
	\item 
	\begin{lstlisting}[numbers=none]
def fonc5(n):
    a = 10
    while n > 0:
        a = a + 1
        n = n - 1
    return a
\end{lstlisting}
	
	Quelle est la valeur renvoyée par \verb+fonc5(6)+? \dotfill
	
	
	\newpage
	
	\item En utilisant vos connaissances et les structures que vous connaissez, proposez une fonction nommée \verb+recherche_maximum(une_liste:list)+ qui renvoie la valeur maximum d'une liste passée en argument.
	
\begin{lstlisting}[numbers=none]
def recherche_maximum(une_liste):
		''' Fonction renvoyant la valeur maximale d'une liste
		Entrée:
		- une_liste: liste de n valeurs de type int
		Sortie:
		- maximum: maximum de la liste (type int) '''
















		return maximum
		
ma_liste = [3,6,8,1,9,4,7]
assert recherche_maximum(ma_liste) == 9
\end{lstlisting}	
	
	
	\bigskip
	
	\item En utilisant vos connaissances et les structures que vous connaissez, proposez une fonction nommée \verb+somme_liste_pairs(une_liste:list)+ qui renvoie la somme des éléments pairs d'une liste passée en argument.
	
\begin{lstlisting}[numbers=none]
def somme_liste_pairs(une_liste):
		
		









		
		
		
	return somme_pairs
	
ma_liste = [3,6,8,1,9,4,7]
assert somme	_liste_pairs(ma_liste) == 18
		
		
		
\end{lstlisting}	

\end{enumerate}


\newpage

\begin{center}\begin{large}
DS03B - Structures IF / FOR / WHILE
\end{large}\end{center}



\begin{enumerate}
	\item 
	
	\begin{lstlisting}[numbers=none]
def fonc1(a,b):
    if a < b:
        return b - a
    else:
        return a * b
\end{lstlisting}
	
	Que renvoie la commande \verb+fonc1(2,3)+ ? \dotfill
	
	\medskip
	
		Que renvoie la commande \verb+fonc1(3,2)+ ? \dotfill

	\item 
	\begin{lstlisting}[numbers=none]
def fonc2(n):
    a = 1
    for i in range(n):
        a = a * 2
    return a
\end{lstlisting}
	
	Que renvoie la commande \verb+fonc2(3)+ ? \dotfill

	\medskip
	
	\item 
	\begin{lstlisting}[numbers=none]
def fonc3(une_liste):
    a = 0
    for elt in une_liste:
        a = a + elt
    return a
\end{lstlisting}
	
	Quelle est la valeur renvoyée par \verb+fonc3([1,2,4,6])+? \dotfill

	\medskip
	
	\item 
	\begin{lstlisting}[numbers=none]
def fonc4(une_liste):
    n = len(une_liste)
    sortie = []
    for i in range(n):
        if une_liste[i] % 2 == 0:
            sortie.append(i)
    return sortie
\end{lstlisting}
	
	Quelle est la valeur renvoyée par \verb+fonc4([1,2,4,6])+? \dotfill
	
	\medskip
	
	\item 
	\begin{lstlisting}[numbers=none]
def fonc5(n):
    a = 10
    while n > 0:
        a = a + 1
        n = n - 1
    return a
\end{lstlisting}
	
	Quelle est la valeur renvoyée par \verb+fonc5(5)+? \dotfill
	
	
	\newpage
	
	\item En utilisant vos connaissances et les structures que vous connaissez, proposez une fonction nommée \verb+recherche_minimum(une_liste:list)+ qui renvoie la valeur minimum d'une liste passée en argument.
	
\begin{lstlisting}[numbers=none]
def recherche_minimum(une_liste):
		''' Fonction renvoyant la valeur maximale d'une liste
		Entrée:
		- une_liste: liste de n valeurs de type int
		Sortie:
		- minimum: minimum de la liste (type int) '''
















		return minimum
		
ma_liste = [3,6,8,1,9,4,7]
assert recherche_minimum(ma_liste) == 1
\end{lstlisting}	
	
	
	\bigskip
	
	\item En utilisant vos connaissances et les structures que vous connaissez, proposez une fonction nommée \verb+somme_liste_impairs(une_liste:list)+ qui renvoie la somme des éléments impairs d'une liste passée en argument.
	
\begin{lstlisting}[numbers=none]
def somme_liste_impairs(une_liste):
		
		









		
		
		
	return somme_impairs
	
ma_liste = [3,6,8,1,9,4,7]
assert somme	_liste_impairs(ma_liste) == 20
		
		
		
\end{lstlisting}	

\end{enumerate}

\end{document}
